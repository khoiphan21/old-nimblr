#**
The value of 'payload' after the template has been evaluated
will be passed as the event to AWS Lambda.
*#
#set($payload = $context.args)
#set($payload.identity = $context.identity)
{
  "version" : "2017-02-28",
  "operation": "Invoke",
  "payload": $util.toJson($payload)
}