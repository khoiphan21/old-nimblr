import { EventDetector } from './event-detector';

describe('EventDetector', () => {
  it('should create an instance', () => {
    expect(new EventDetector()).toBeTruthy();
  });

  describe('detectEvent', () => {
    // TODO: @Bruno not impl
    it('should map to ...', done => {

    });
  });
});
